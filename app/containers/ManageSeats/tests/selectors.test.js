// import { fromJS } from 'immutable';
// import { makeSelectManageSeatsDomain } from '../selectors';

// const selector = makeSelectManageSeatsDomain();

describe('makeSelectManageSeatsDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
