// import { fromJS } from 'immutable';
// import { makeSelectCreateBillDomain } from '../selectors';

// const selector = makeSelectCreateBillDomain();

describe('makeSelectCreateBillDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
