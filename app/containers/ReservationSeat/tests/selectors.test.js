// import { fromJS } from 'immutable';
// import { makeSelectReservationSeatDomain } from '../selectors';

// const selector = makeSelectReservationSeatDomain();

describe('makeSelectReservationSeatDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
